#writing a defition with the funtion foo and the variable var haiving the value 5
def foo(var = 5):
#increment the variable var by 1.1
    var +=1.1
#using the return statement to return the variable var with the incremented value 
    return var
#using the statement print to print the varaible foo
print(foo(),foo(1))
